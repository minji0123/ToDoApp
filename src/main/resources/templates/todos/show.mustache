{{>layouts/header}}

<div class="todo-list show-list">
<table class="table ">
    <thead>
    <tr>
        <th scope="col">번호</th>
        <th scope="col">할일</th>
        <th scope="col">내용</th>
    </tr>
    </thead>
    <tbody>

    <!--model.addAttribute data-->
    {{#todo}}
        <tr>
            <th scope="row">{{id}}</th>
            <td>{{title}}</td>
            <td>{{content}}</td>
    {{/todo}}
    <!--model.addAttribute data-->

    </tr>
    </tbody>
</table>
<!--    <div class="d-grid gap-2 d-md-flex justify-content-md-end">-->
<!--        <a href="/todos/{{todo.id}}/edit" class="btn btn-outline-dark">수정</a>-->
<!--        <a href="/todos/{{todo.id}}/delete" class="btn btn-outline-danger">지우기</a>-->
        <a href="/todos" class="float-r-style">back</a>
    <div class="clear-style"></div>
<!--    </div>-->
</div>

{{>layouts/footer}}